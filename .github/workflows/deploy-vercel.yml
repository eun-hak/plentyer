# main 브랜치 push 시 Vercel Deploy Hook 호출 (Git author 권한 이슈 우회)
name: Deploy to Vercel

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel Deploy
        run: curl -f -X POST "${{ secrets.VERCEL_DEPLOY_HOOK }}"
